_schema-version: "3.2"
ID: emwebapp
description: Generated by Fiori Tools
version: 0.0.1

modules:

- name: emwebapp-destination-content
  type: com.sap.application.content
  requires:
  - name: emwebapp-destination-service
    parameters:
      content-target: true
  - name: emwebapp-repo-host
    parameters:
      service-key:
        name: emwebapp-repo-host-key
  - name: emwebapp-uaa
    parameters:
      service-key:
        name: emwebapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: emwebapp_html_repo_host
          ServiceInstanceName: emwebapp-html5-service
          ServiceKeyName: emwebapp-repo-host-key
          sap.cloud.service: emwebapp
        - Authentication: OAuth2UserTokenExchange
          Name: emwebapp_uaa
          ServiceInstanceName: emwebapp-xsuaa-service
          ServiceKeyName: emwebapp-uaa-key
          sap.cloud.service: emwebapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true

- name: emwebapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: emwebapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - emwebapp.zip
      name: emwebapp
      target-path: resources/

- name: emwebapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []


resources:
- name: emwebapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: emwebapp-destination-service
    service-plan: lite

- name: emwebapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: emwebapp-xsuaa-service
    service-plan: application
    
- name: emwebapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: emwebapp-html5-service
    service-plan: app-host

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
